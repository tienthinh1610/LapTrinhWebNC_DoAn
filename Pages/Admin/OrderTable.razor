<table class="table table-sm table-striped table-bordered">
    <thead>
        <tr><th colspan="5" class="text-center">@TableTitle</th></tr>
    </thead>
    <tbody>
        @if (Orders?.Count() > 0) {
            @foreach (Order o in Orders) {
                <tr>
                    <td>@o.Name</td>
                    <td>@o.Zip</td>
                    <th>Sản phẩm</th> 
                    <th>SL & Size</th> 
                    <td>
                        <button class="btn btn-sm btn-danger"
                                @onclick="@(e => OrderSelected.InvokeAsync(o.OrderID))">
                            @ButtonLabel
                        </button>
                    </td>
                </tr>
                
                @* Lặp qua chi tiết từng dòng đơn hàng *@
                @foreach (OrderLine line in o.Lines) { 
                    <tr>
                        <td colspan="2"></td>
                        
                        @* Hiển thị Tên Sản phẩm *@
                        <td>
                            <span class="fw-bold">@line.ProductName</span>
                        </td>
                        
                        @* 🌟 ĐIỀU CHỈNH: Hiển thị Size và Quantity rõ ràng hơn *@
                        <td>
                            @line.Quantity x 
                            <span class="badge bg-info text-dark">Size: @line.ProductSize</span>
                        </td>
                        <td></td>
                    </tr>
                }
            }
        } else {
            <tr><td colspan="5" class="text-center">Không có đơn hàng nào</td></tr>
        }
    </tbody>
</table>

@code {
    [Parameter]
    public string TableTitle { get; set; } = "Orders";

    [Parameter]
    public IEnumerable<Order> Orders { get; set; } = Enumerable.Empty<Order>();

    [Parameter]
    public string ButtonLabel { get; set; } = "Ship";

    [Parameter]
    public EventCallback<int> OrderSelected { get; set; }
}